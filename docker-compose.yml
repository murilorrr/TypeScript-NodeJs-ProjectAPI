version: "3"

services:
  app:
    build: .
    command: npm install
    ports:
      - "3000:3000"
    environment:
      - HOST=localhost
    volumes:
      - "$PWD/src"

  database:
    image: mongo
    ports:
      - "27017:27017"